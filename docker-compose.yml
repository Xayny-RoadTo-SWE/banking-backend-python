version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-server
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: geocbn1709
      MYSQL_DATABASE: ebanx_bank
      MYSQL_USER: root
      MYSQL_PASSWORD: geocbn1709
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: